/***************************************
 * 求出x的偶数因子
 * 并按从小到大的顺序放在pp所指的数组中
 * 输入 : 整数x
 * 输出 : 偶数因子数组pp
 * 输出 : 因子个数n
 ***************************************/
void func15(int x, int* pp, int& n)
{
	for (int i = 0; i < x / 2; i++)//开始循环
		int n = 0;//因子个数
		do
		{
			*(pp + n) = i;
			n++;
		} while (x%i == 0 && i % 2 == 0);//判断是否满足条件，列入pp数组中；
		cout << *pp;//循环结束，输出数组；
		cout << n << endl;//输出因子个数
		return 0;
}